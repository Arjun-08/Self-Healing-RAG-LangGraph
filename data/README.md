# Data

`sample_company_handbook.txt` is a small self-contained knowledge base for demonstrating the application.

`evaluation.json` contains example benchmark questions with expected answers and transparent keyword checks.

For a serious portfolio result, replace or extend the example benchmark with 100–300 manually verified questions and annotate the relevant chunks and answerability of each question.
